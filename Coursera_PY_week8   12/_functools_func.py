import functools

# partial
            # prints = functools.partial(print, end=' ')
            # prints(1)
            # prints(2)

# reduce()
            # print(functools.reduce(lambda x, y: x ** y, [5, 2, 3]))
